# Pot 2.7.2 静态分析

分析对象：`C:\Users\26320\Desktop\pot.exe`

## 文件信息

- 文件大小：41,570,304 字节
- SHA-256：`54846CC6DFEB3DF51BAEDA1071B8DD522BE6C89BCDB6AD8C72012D4F52C9D040`
- 文件版本：2.7.2
- 产品描述：Pot App
- PE 架构：x86（`0x014C`）
- PE 时间戳：2023-12-14 08:42:27 UTC
- Authenticode：未签名

## 技术栈线索

二进制字符串显示该版本使用：

- Tauri 1.5.2
- `tauri-runtime` / `tauri-runtime-wry` 0.14.1
- WebView2
- Rust 原生截图、全局快捷键和系统 OCR 模块

## 可见功能线索

静态字符串中可识别出以下命令和菜单项：

- 输入翻译、划词翻译
- 剪贴板监听
- OCR 识别、OCR 翻译
- 屏幕截图与区域裁剪
- 多翻译插件与语言检测
- 代理、字体、托盘、快捷键与更新

## LinguaLens 的改进目标

1. 将最高频的“截取 → OCR → 翻译”压缩为一个快捷键和一次框选。
2. OCR 默认在本机执行，截图只保存在内存中。
3. 翻译结果可直接覆盖到原屏幕区域，而不只显示在独立窗口。
4. 同时支持免配置即时翻译、OpenAI 兼容服务和本地 Ollama。
5. API Key 使用 Windows 安全存储，而不是明文写入配置文件。
6. 采用当前显示器捕获，正确处理多显示器坐标与高 DPI 缩放。

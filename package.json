{
  "name": "lingualens",
  "version": "0.1.0",
  "description": "Fast, privacy-minded screen OCR and translation for Windows.",
  "main": "main.js",
  "author": "Codex",
  "license": "MIT",
  "scripts": {
    "start": "electron .",
    "test": "node --test tests/*.test.js",
    "check": "node --check main.js && node --check preload.js && node --check lib/frame-signature.js && node --check lib/live-translation-session.js && node --check lib/ocr-layout.js && node --check lib/ocr-service.js && node --check lib/settings-store.js && node --check lib/translation-service.js && node --check lib/tray-icon.js && node --check src/capture.js && node --check src/result.js && node --check src/translation-overlay.js",
    "dist": "electron-builder --win portable --config.electronDist=node_modules/electron/dist"
  },
  "dependencies": {
    "tesseract.js": "7.0.0"
  },
  "devDependencies": {
    "electron": "43.2.0",
    "electron-builder": "26.15.3"
  },
  "build": {
    "appId": "com.codex.lingualens",
    "productName": "LinguaLens",
    "asar": true,
    "files": [
      "main.js",
      "preload.js",
      "lib/**/*",
      "src/**/*",
      "package.json"
    ],
    "win": {
      "target": [
        {
          "target": "portable",
          "arch": [
            "x64"
          ]
        }
      ],
      "artifactName": "LinguaLens-${version}-portable.${ext}",
      "icon": "build/icon.png"
    }
  },
  "allowScripts": {
    "electron@43.2.0": true
  }
}
